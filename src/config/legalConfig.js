// Configuración de Términos y Privacidad.
// Si se define una URL, se abre externamente (navegador del sistema).
// Si se deja en null, se muestra la pantalla interna con el texto legal.
export const legalConfig = {
    privacyPolicyUrl: null,
    termsAndConditionsUrl: null,
};
